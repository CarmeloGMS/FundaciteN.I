---
const videos = [
  { id: 'dQw4w9WgXcQ', title: 'Video de ejemplo (YouTube)' }
];
--- 
<div class="grid grid-cols-1 md:grid-cols-2 gap-4">
  {videos.map(v => (
    <div class="rounded-lg overflow-hidden bg-black">
      <iframe
        class="w-full h-56"
        src={`https://www.youtube.com/embed/${v.id}`}
        title={v.title}
        frameborder="0"
        allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
        allowfullscreen
      />
    </div>
  ))}

  <div class="bg-black text-white p-2 rounded-lg">
    <video controls class="w-full h-56 object-cover rounded-md">
      <source src="https://interactive-examples.mdn.mozilla.net/media/cc0-videos/flower.mp4" type="video/mp4" />
      Tu navegador no soporta video.
    </video>
    <div class="pt-2 text-sm text-slate-300">Video local de ejemplo</div>
  </div>
</div>